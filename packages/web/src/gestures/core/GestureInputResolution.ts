/**
 * Specifies the possible resolutions for dispositions made to input.
 * - `accept` — The input can be used to produce a gesture.
 * - `reject` — The input has been rejected.
 */
export type GestureInputResolution = "accept" | "reject";
