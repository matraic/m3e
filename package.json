{
  "workspaces": [
    "packages/core",
    "packages/app-bar"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "cem": "npm run cem --workspaces",
    "lint": "npm run lint --workspaces",
    "all": "npm run build && npm run cem && npm run lint",
    "clean": "npm run clean --workspaces"
  }
}
